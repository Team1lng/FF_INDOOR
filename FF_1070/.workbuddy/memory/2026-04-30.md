# 2026-04-30 工作日志

## 对讲串口 RX 异常分析 — Codex 修改对比审查

用户使用 OpenAI Codex (GPT-5.4) 对内线串口 RX 异常做了代码修改，修改了 3 个文件：
1. intercom_interface.c — 加 stash 环形缓冲区保护非回显字节，UartClean 改为 tcflush
2. intercom.c — SerialGetMessage 加 byte-by-byte 重试，读到 -1 丢弃整帧
3. layout_logo.c — 启动顺序改为先注册接口再起线程

Codex 修改评估：方向正确但遗漏了最关键根因 — uart_ctrl.c 中 VTIME=10（1秒超时）导致 uart_read 阻塞。
详见本次对话的完整对比分析。
